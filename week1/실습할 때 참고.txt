cd 잘 써서 적절한 터미널로 이동하는 거 잊지말고 

tsc --showConfig로 확인도 한번 해주고 

tsc 해서 js파일 만든 다음에
node ./dist/test.js 써서 console.log("")값 확인해주며 실습하자

<리터럴(매우 매우 엄격 값까지 고정) 타입 / 객체 리터럴 타입 / 배열 타입 / 튜플 타입 / 유니언 타입(두 개이상의 타입 가능하게)> 중요

    인덱스 시그니처 - 객체에 추가적인 프로퍼티를 허용
    선택적 프로퍼티 만들 수 있음
    읽기 전용(Readonly) 프로퍼티

유니언과 리터럴타입 결합해서
function move(direction: "left" | "right" | "up" | "down") {
    console.log(`You moved: ${direction}`);
} 이런 식을 "left" | "right" | "up" | "down"만 쓸 수 있게끔 하기도 


Merging Interfaces(인터페이스 병합)

interface UMC {
	name: string;
	nickname: string;
}
interface UMC {
	skill: string;
}
let member: UMC = { name: '김용민', nickname: '매튜', skill: 'WEB' };
이런 거 가능함 꼬이는 거 조심해야함!!
namespace A {
  const a = 1;
  export let b = a + 10;
}

namespace A {
  export const c = 2;
  b = 20;
}

console.log(A.c);

근데 예시에 이거 충분히 A.b가 꼬인 거 아닌가? 위에선 11 밑에선 20인데..?


제네릭 -  외부에서 원하는 타입을 지정, 뒤에 타입이 지정되는 너낌?

Enum - 내가 아는거랑 비슷함
enum Role {
  Admin = 1,
  User,
  Guest
}
console.log(Role[1]); // "Admin"
console.log(Role.Admin); // 1